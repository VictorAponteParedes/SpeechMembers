export type nameType = {
    id: number | string;
    name: string;
    age: string;
    email: string;
    phome: string;
    city: string;
    registrationDate: string;
    calling: string;
}

export type nameMemberChage ={
    name: string
}