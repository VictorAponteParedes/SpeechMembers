import {Text, View} from 'react-native';

const ProfileScreen = () => {
  return (
    <View style={{flex: 1}}>
      <Text style={{color: 'black'}}>Mi perfil</Text>
    </View>
  );
};

export default ProfileScreen;
