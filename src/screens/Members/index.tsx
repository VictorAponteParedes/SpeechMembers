import {Text, View} from 'react-native';

const MemberScreen = () => {
  return (
    <View style={{flex: 1}}>
      <Text style={{color: 'black'}}>Bienvenido Miembro</Text>
    </View>
  );
};

export default MemberScreen;
